export default function RenewingSoonBadge({ daysLeft }) {
  return (
    <span className="inline-flex items-center ml-2 px-2 py-0.5 rounded text-xs font-medium bg-[var(--warning)]/15 text-[var(--warning)]">
      Renewing Soon
    </span>
  );
}
